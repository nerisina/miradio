import React from 'react';

const PlayButton = props => (
  <div>
      <button className="play-button bg-transparent contain button-reset play-button big-contain bg-center center">Play</button>
  </div>
);

export default PlayButton;